/*
 * -------------------------------------------------
 *  Nextflow config file for running tests
 * -------------------------------------------------
 * Defines bundled input files and everything required
 * to run a fast and simple test. Use as follows:
 *   nextflow run nf-core/scovtree -profile test_gisiad,<docker/singularity>
 */

params {
  config_profile_name = 'Test profile'
  config_profile_description = 'Minimal test dataset to check pipeline function'
  // Limit resources so that this can run on GitHub Actions
  max_cpus             = 2
  max_memory           = 2.GB
  max_time             = 48.h

  // Input data
  input                = 's3://scovtree/example_sequences_aus.fasta'
  reference_name       = 'MN908947.3'
  reference_fasta      = 's3://scovtree/nCoV-2019.reference.fasta'
  substitution_model   = 'GTR'

  //Input options for filtering sequence against GISIAD Database
  filter_gisaid        = false
}
